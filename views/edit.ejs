<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Chat</title>
</head>
<body>
    <h3>Edit your message : <%= chat.id %></h3>
    <p>This mesage is sent by <b><%= chat.from%></b> to <b><%= chat.to%></b></p>

    <!-- npm i method-override -->
    <form method="POST" action="/chats/<%= chat.id%>?_method=PUT">
        <textarea name="msg" rows="2" cols="50"><%= chat.msg%></textarea> <br><br>

        <button>Edit</button>
    </form>
</body>
</html>